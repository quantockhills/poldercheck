[theme]
base = "light"
primaryColor = "#4a4a6a"
backgroundColor = "#f8f4f0"
textColor = "#1a1a2e"

[server]
address = "0.0.0.0"
port = 8501
headless = true        # suppresses the "you can now view..." browser-open attempt

[browser]
gatherUsageStats = false
